# DAP DBMS 아키텍처 암기용 요약본 (A4 1장)

## 1. DBMS 아키텍처 핵심 구성
- **질의 처리기(Query Processor)**: SQL 해석, 검증, 최적화, 실행
- **저장 관리자(Storage Manager)**: 데이터 파일, 인덱스, 버퍼, 로그 관리
- **트랜잭션 관리자(Transaction Manager)**: ACID 보장, 동시성 제어
- **복구 관리자(Recovery Manager)**: 장애 시 UNDO/REDO 수행
- **카탈로그(데이터 사전)**: 메타데이터, 통계정보 관리

---

## 2. SQL 처리 순서
**파싱 → 의미 분석 → 최적화 → 실행**

- **파싱**: 문법 검사, 구문 트리 생성
- **의미 분석**: 테이블/컬럼 존재 여부, 권한, 타입 확인
- **최적화**: 최소 비용 실행계획 선택
- **실행**: 실제 데이터 접근 및 결과 반환

### 자주 나오는 접근/조인 방식
- **접근 방식**
  - Full Table Scan
  - Index Scan
  - Index Range Scan
  - Index Unique Scan

- **조인 방식**
  - **Nested Loop Join**: 작은 집합 + 인덱스 있을 때 유리
  - **Sort Merge Join**: 정렬 후 병합, 대량 처리
  - **Hash Join**: 해시 기반, 등가 조인에 유리

---

## 3. 저장 구조 핵심
**데이터베이스 → 테이블스페이스 → 세그먼트 → 익스텐트 → 페이지(블록)**

- **페이지/블록**: DBMS I/O의 기본 단위
- DBMS는 **행 단위가 아니라 페이지 단위**로 읽고 씀
- 성능 문제는 대부분 **디스크 I/O 최소화**와 관련됨

### 파일 종류
- **데이터 파일**: 실제 테이블 데이터
- **인덱스 파일**: 검색 성능 향상
- **로그 파일**: 복구용 변경 이력
- **임시 파일**: 정렬, 해시 작업

---

## 4. 인덱스 핵심
### B-Tree 인덱스
- 가장 일반적
- **정렬 유지**
- **범위 검색에 강함**
- 루트/브랜치/리프 구조

### Bitmap 인덱스
- 값 종류가 적은 컬럼에 적합
- DW/분석계에 유리
- DML 많은 OLTP에는 비효율

### 시험 암기
- **B-Tree = OLTP / 범위검색**
- **Bitmap = OLAP / 저카디널리티**

---

## 5. 버퍼 관리자 핵심
- 디스크 페이지를 메모리에 적재해 재사용
- 디스크 I/O 감소가 목적

### 핵심 용어
- **Buffer Cache**
- **Dirty Page**: 메모리에서 수정됐지만 디스크 미반영
- **Page Replacement**: 버퍼 부족 시 교체
- **LRU**: 대표적 교체 방식

### 중요 비교
- **논리적 읽기(Logical Read)**: 메모리에서 읽음
- **물리적 읽기(Physical Read)**: 디스크에서 읽음

> 시험 포인트: **물리적 읽기를 줄이는 것이 성능 향상의 핵심**

---

## 6. 트랜잭션 핵심
### 트랜잭션
- 하나의 논리적 작업 단위
- 예: 계좌이체, 주문처리

### ACID
- **Atomicity(원자성)**: 전부 성공 또는 전부 실패
- **Consistency(일관성)**: 무결성 유지
- **Isolation(격리성)**: 동시에 실행되어도 상호 간섭 최소화
- **Durability(지속성)**: 커밋 후 영구 반영

> **ACID는 무조건 암기**

---

## 7. 동시성 제어 핵심
### 대표 문제
- **Lost Update**: 갱신 손실
- **Dirty Read**: 미커밋 데이터 읽음
- **Non-Repeatable Read**: 같은 행 재조회 결과 달라짐
- **Phantom Read**: 조건 검색 결과 행 집합 달라짐

### 락(Lock)
- **공유락(S Lock)**: 읽기
- **배타락(X Lock)**: 쓰기

### 락 단위
- DB / 테이블 / 페이지 / 행(Row)

> **락 단위 작을수록 동시성 ↑, 관리 비용 ↑**

### 교착상태(Deadlock)
- 서로 상대방 락 해제를 기다리며 무한 대기
- 탐지 후 한 트랜잭션 종료로 해소

---

## 8. 복구 핵심
### WAL (Write-Ahead Logging)
- **데이터를 디스크에 쓰기 전에 로그를 먼저 기록**
- 복구의 핵심 원칙

### 복구 기법
- **UNDO**: 미커밋 작업 취소
- **REDO**: 커밋된 작업 재반영

### 체크포인트(Checkpoint)
- 특정 시점 상태 저장
- 장애 복구 시간 단축

### 장애 유형
- 트랜잭션 장애
- 시스템 장애
- 미디어 장애

> **WAL + UNDO + REDO + Checkpoint = 복구 핵심 4종**

---

## 9. 카탈로그(데이터 사전)
- 테이블, 컬럼, 인덱스, 제약조건, 권한, 통계정보 저장
- **최적화기**가 실행계획 수립 시 활용

---

## 10. 3단계 스키마 / 데이터 독립성
### 3단계 스키마
- **외부 스키마**: 사용자 관점
- **개념 스키마**: 전체 논리 구조
- **내부 스키마**: 물리 저장 구조

### 데이터 독립성
- **논리적 데이터 독립성**: 개념 변화가 외부에 영향 최소화
- **물리적 데이터 독립성**: 내부 변화가 개념에 영향 최소화

> 시험에서 DBMS 아키텍처와 데이터 독립성을 연결해서 자주 출제

---

## 11. OLTP vs OLAP
| 구분 | OLTP | OLAP |
|---|---|---|
| 목적 | 거래 처리 | 분석/집계 |
| 특징 | 짧고 빈번한 트랜잭션 | 대량 조회 |
| 구조 | 정규화 | 비정규화/스타 스키마 |
| 인덱스 | B-Tree | Bitmap 가능 |
| 중요 요소 | 동시성, 응답속도 | 스캔, 집계, 병렬처리 |

---

## 12. 초압축 암기 문장
- **SQL 처리:** 파싱 → 의미 분석 → 최적화 → 실행
- **저장 단위:** 세그먼트 → 익스텐트 → 페이지(블록)
- **I/O 기본 단위:** 페이지/블록
- **대표 인덱스:** B-Tree
- **버퍼 핵심:** 논리 읽기 vs 물리 읽기
- **트랜잭션 핵심:** ACID
- **동시성 문제:** Lost Update, Dirty Read, Non-Repeatable Read, Phantom Read
- **복구 핵심:** WAL, Checkpoint, UNDO, REDO
- **데이터 독립성:** 논리적 / 물리적

---

## 13. 시험 직전 최종 암기 키워드
**질의처리기 / 저장관리자 / 버퍼 / 인덱스 / ACID / 락 / 데드락 / WAL / UNDO / REDO / 카탈로그 / 3단계 스키마**

원하시면 다음으로  
**"이 요약본을 더 압축한 암기카드 버전"** 또는  
**"서술형 답안용 10줄 요약"**으로 만들어드릴게요.
